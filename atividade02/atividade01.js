// 1. Faça um script que verifique se uma letra digitada num 
// campo de input é vogal ou consoante.

const isVowel = function(char) {
    let charLower = char.toLowerCase();
    if(charLower === 'a' || charLower === 'e' || charLower === 'i' || charLower === 'o' || charLower === 'u'){
        return minusculo = console.log("A letra: " + char + " é uma vogal");
    }
    return minusculo = console.log("A letra: " + char + " é uma consoante");
}

let letra = isVowel('A');

